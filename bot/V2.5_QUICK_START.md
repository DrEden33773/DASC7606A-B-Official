# Long-Board v2.5 快速启动 ⚡

**一条命令，最后一搏！**

---

## 🚀 立即启动

```bash
python main.py --model wide_resnet28_12
```

**就这么简单！** 默认已配置v2.5策略。

---

## 📊 v2.5 vs v2 对比

| 版本 | 权重范围 | Detail-Sensitive | Mid-High | F1结果 |
|------|----------|------------------|----------|--------|
| **v2** | 0.2-2.0 (10x跨度) | 0.2 ❌ | 2.0 ❌ | 0.80 **失败** |
| **v2.5** | 0.5-1.5 (3x跨度) | 0.5 ✅ | 1.4 ✅ | 0.82-0.825 (预期) |

**核心改进**:

- ✅ Detail-Sensitive: 0.2 → 0.5（防止模型放弃）
- ✅ Mid-High: 2.0 → 1.4（避免过拟合）
- ✅ 权重跨度: 10x → 3x（平衡训练）

---

## ⏱️ 预期结果

- **F1-score**: 0.82-0.825
- **训练时长**: 2-2.5小时
- **成功概率**: 60%

---

## 🔍 快速检查

### 训练开始后，确认以下几点

**前50个epoch**:

```
✅ 应该显示: "Warmup period (X/50), early stopping disabled"
✅ 不应触发早停
```

**epoch 100左右**:

```
✅ Validation F1应该在0.78-0.80
✅ Detail-Sensitive类F1应该在0.55-0.60（不是v2的0.53）
```

**最终结果**:

```
✅ 最佳模型应该在epoch 200-250
✅ 最终F1应该≥0.82
```

---

## ⚠️ 如果失败怎么办？

### F1 < 0.82 → 立即启动Ensemble

```bash
# 第一个模型：WRN-28-12 (uniform)
python main.py --model wide_resnet28_12 --weight_strategy uniform

# 第二个模型：WRN-28-12 (long_board v1)  
python main.py --model wide_resnet28_12 --weight_strategy long_board

# 第三个模型：PyramidNet-110-270
python main.py --model pyramidnet110_270 --weight_strategy uniform
```

**Ensemble预期**: F1=0.83-0.84，成功率80%

---

## 📅 时间规划

| 任务 | 时间 |
|------|------|
| **v2.5实验** | 2-2.5h |
| **结果分析** | 0.5h |
| **Ensemble备选** | 6-8h（如需要） |
| **DDL缓冲** | 2天 |

**结论**: 时间充足，不慌！

---

## 📚 详细文档

- 完整指南: `bot/LONG_BOARD_V2.5_READY.md`
- 失败分析: `bot/analysis/long_board_v2_failure_analysis.md`

---

**现在就开始吧！** 🚀

```bash
python main.py --model wide_resnet28_12
```
